# 龙虾建议：补充决策确认

本文记录用户对第二轮 10 项问题的逐项确认，补充此前《龙虾建议-逐条回应汇总》。与此前决定不一致的部分，以本轮明确确认的内容为准。

本文是决策记录，不表示相关代码、数据库或 API 契约已经修改，也不表示已核验模型配置。

## 1. 模型选择

**决定：沿用已配置的 Gemini 3.6 Flash。** 将此前第 5 项“模型及调用方式待提供”中的模型选择更新为已确定。

模型名称沿用本轮问题及用户确认的表述；实际配置、模型标识和调用方式仍需在实施时核查，本文未核验这些技术事实。

## 2. API-23 与视觉功能

**决定：保留 API-23 的接口实现，但禁用视觉分析功能。** 调用时返回明确、在 API 契约中约定的“功能已禁用”响应。

具体响应字段、状态码和错误码应在实现前对齐契约，本轮未指定这些细节。

## 3. API-27 及其他视觉接口

**决定：视觉部分统一按第 2 项处理。** 用户原话：“视觉部分都同第2项的回复”。

因此 API-27 也采用保留接口、禁用视觉功能、返回明确禁用响应的方式，不采用原建议中直接排除 API-27 的方案。此前要求的 API-01 至 API-26 实现范围仍保留，其中视觉功能按本轮决定禁用。

## 4. 私密调解期间的音频隔离

**决定：进入私密调解时断开 LiveKit 共享音频连接，返回时使用新令牌重连。**

- 全员同意进入调解后，所有人断开与共享音频房间的连接，进入各自的私密调解房间。
- 全员同意返回会议后，重新签发令牌并连接共享音频房间。

## 5. 浏览器语音识别不可用或未识别到内容

**决定：不提供手动转写输入；识别不到的内容直接忽略。** 不使用预设转写文本填补缺失内容。

用户原话：“选2，如果识别不到就忽略”。

## 6. 私聊保留与删除

**决定：保存私密调解聊天，以支持刷新后恢复。**

- 参与者只能访问自己的私聊。
- 后端和 LLM 按调解需要处理私聊内容，仍须遵守此前确认的共享隐私边界。
- 房间删除或消息保存满 24 小时即删除，以先到者为准。
- 不保存原始音频。

## 7. 调解关联节点的发现

**决定：在 API-03 和 Realtime 房间事件中增加 `activeMediationNodeId`。** 用于告诉客户端本次调解关联哪个节点。

该字段不改变调解发起方或同意流程：仍由 LLM 判断达到阈值后发起提议，全员同意后进入调解。本项将此前第 20 项关于节点标识字段的待定状态更新为同意增加。

## 8. 自动触发规则

**决定：LLM 返回 0–1 的结构化争议分数 `contention`，针对被评估节点同时满足以下条件时，发起全员同意投票：**

- `contention >= 0.72`。
- 至少有两名参与者发言。
- 至少有三条最终转写片段。

“最终转写片段”指语音识别已确认的文本，不包含识别中的临时结果。达到条件只触发调解提议，不能跳过全员同意直接进入调解。

## 9. 共享共识树的生成、更新与保存

**决定：进入私密调解时生成共享共识树，此后每次私密交流成功提取为结构化信息后更新。** 在调解会话中保存带版本号的 JSON 树。

延续此前第 21 项设定：树基于数据库中的表面冲突，展示可共享的他人观点与 LLM 推断的底层共识，所有人在各自私密房间中查看同一棵共享树。

共享树继续遵守隐私边界，不包含原始私聊或详细情绪数据，也不逐字公开私密解读。LLM 推断的共识应与参与者已确认的共识明确区分。

## 10. 等待界面参考期间的开发安排

**决定：先推进后端结构和业务逻辑，界面视觉大改等待用户提供 demo。**

本项确认可以先行推进的工作范围；本文仅记录决定，未执行后端开发或视觉修改。

## 第三轮架构确认

用户后续明确确认以下四项推荐，详细契约见
[`docs/api/decision-overrides-v0.2.md`](docs/api/decision-overrides-v0.2.md)：

1. 浏览器只将 final STT 结果通过 LiveKit data packet 发送；Worker 校验发送者身份后调用 API-22。
2. API-24 分析成功后自动为所有 active participants 创建调解提议；API-13 保留兼容实现，但 UI 不提供手动入口。
3. API-23 与 API-27 统一返回 `409 FEATURE_DISABLED`；`visualAffect` 固定为 false 且 UI 隐藏。
4. API-15 返回完整版本化共识树；API-17 返回最新树版本；Realtime 通知版本变化，客户端重新读取 API-15。

最终实现范围为 API-01～27，其中 API-23 与 API-27 保留但禁用。
